# Production Environment Configuration
APP_ENV=production
NODE_ENV=production
LOG_LEVEL=INFO

# Database Configuration (Production)
POSTGRES_DB=dendreo_prod_db
POSTGRES_USER=dendreo_user
POSTGRES_PASSWORD=your_secure_production_password_here
DATABASE_URL=postgresql://dendreo_user:your_secure_production_password_here@postgres:5432/dendreo_prod_db

# API Configuration
DENDREO_API_KEY=your_dendreo_api_key_here
DENDREO_BASE_URL=https://pro.dendreo.com/competences_et_metiers/api

# HubSpot Configuration
HUBSPOT_API_KEY=your_hubspot_api_key_here

# Sync Configuration
SYNC_SCHEDULE=0 8 * * *       # Daily at 8AM
SYNC_LOG_LEVEL=INFO           # Sync logging level
DENDREO_ADF_LIMIT=            # Optional: limit ADFs for testing
SYNC_API_KEY=                 # Optional: protect sync API endpoints

# Frontend Configuration (Production)
REACT_APP_API_URL=/api
FRONTEND_PORT=3000
BACKEND_PORT=8000

# Security Configuration
# Generate strong passwords for production
SECRET_KEY=your_very_long_secret_key_for_production_here
JWT_SECRET=your_jwt_secret_for_production_here

# SSL Configuration (if using HTTPS)
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# Monitoring and Logging
SENTRY_DSN=your_sentry_dsn_for_error_tracking
LOG_FILE_PATH=/app/logs/production.log

# GitHub Repository (update with your actual repo)
GITHUB_REPO=https://github.com/YOUR_USERNAME/DendreoProgression.git
PROD_BRANCH=main

# Redis Configuration (for caching)
REDIS_URL=redis://redis:6379/0
REDIS_ENABLED=true

# Production Performance Settings
POSTGRES_SHARED_BUFFERS=256MB
POSTGRES_EFFECTIVE_CACHE_SIZE=1GB
POSTGRES_MAX_CONNECTIONS=200

# Application Settings
HOST=0.0.0.0
PORT=8000

# Deployment Settings
DEPLOY_BRANCH=main
BACKUP_RETENTION_DAYS=30